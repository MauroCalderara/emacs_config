# emacs_config
My configuration for emacs / evil / org-mode

Even though I'm a vim user, I use emacs with the 'evil' plugin due to org-mode. The configuration for org-mode is quite adapted to my needs. I publish it here as a reference for others to use freely.
